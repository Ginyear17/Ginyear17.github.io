[project]
name = "ginyear-backend"
version = "0.1.0"
description = "小杰的杂物间 - FastAPI 后端"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
    "psycopg[binary]>=3.3.5",
    "sqlalchemy>=2.0.52",
    "uvicorn[standard]>=0.52.4",
]
